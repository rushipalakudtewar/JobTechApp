// import { GET_DOMAIN } from '@gird/ui';
export const FEATURE_NAME = 'generalPolicyFeature';

// let domain = GET_DOMAIN();

// if (window.location.href.includes('localhost')) domain = 'officegird.com';

export const environment = {
    // apiUrl: 'http://localhost:5000/api/',
    apiUrl: 'https://job-tech-api.vercel.app/api/'
};
